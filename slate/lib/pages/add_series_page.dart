import 'package:flutter/material.dart';

class AddSeriesPage extends StatelessWidget {
  const AddSeriesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Series'),
      ),
      body: const Center(
        // TODO: Implement the add series form here.
        child: Text('This will be the add series form.'),
      ),
    );
  }
}